# AI/SW 기초 미션 결과 보고서

## 과제 요약

이 과제는 Python으로 단순한 기록 프로그램이 아니라 예외 상황에서도 데이터가
안전하고 유지보수할 수 있는 “작은 서비스” 형태의 콘솔 가계부를 만드는
과제이다. 거래를 영구 저장하고 CRUD, 검색, 통계, 예산, 카테고리, CSV 입출력을
제공해야 한다. 제너레이터, 데코레이터, 타입 힌트, 계층별 모듈 분리를 실제
코드에 적용하는 것이 핵심 학습 목표이다.

## 핵심 요구사항

### 목적

1. JSONL 또는 CSV 파일로 데이터를 영구 저장한다.
2. 모델·저장소·서비스·CLI의 책임을 나눈다.
3. `yield` 기반 스트리밍이 대용량 파일에 필요한 이유를 이해한다.
4. 로그·시간 측정 등 공통 관심사를 데코레이터로 분리한다.
5. 타입 힌트로 함수와 데이터의 계약을 명확히 한다.

### 입력·출력 조건

| 기능 | 입력 | 출력 |
|---|---|---|
| add | 날짜, 타입, 카테고리, 금액, 메모, 태그 대화형 입력 | 저장 메시지와 거래 id |
| list | `--limit N` | 날짜 최신순 목록 |
| search | 기간, 카테고리, 타입, 메모 키워드, 태그 | 조건에 맞는 최신순 목록 |
| summary | `--month YYYY-MM`, `--top N` | 수입·지출·잔액·지출 TOP N·예산 상태 |
| budget | `set/get`, 월, 금액 | 저장 또는 조회 결과 |
| category | `add/list/remove` | 카테고리 처리 결과 |
| update | id와 변경 옵션 | 수정 성공 또는 없는 id 오류 |
| delete | `--id` | 삭제 성공 또는 없는 id 오류 |
| import | `--from CSV` | 등록·건너뜀 건수 |
| export | 출력 경로와 월 또는 기간 | CSV와 내보낸 건수 |

거래는 `id`, `type`, `date`, `amount`, `category`, `memo`, `tags` 필드를
가진다. 날짜는 실제 `YYYY-MM-DD`, 타입은 `income/expense`, 금액은 양수
정수, 카테고리는 등록된 값이어야 한다.

### 평가 기준 정리

PDF에 별도 배점표는 없으므로 아래 항목을 기능 요구사항과 제약사항에서 도출한
실질 평가 기준으로 보았다.

1. 필수 10개 기능의 정상·오류 흐름
2. 거래·카테고리·예산을 3개 이상 파일에 영구 저장
3. 목록·검색의 제너레이터 기반 스트리밍과 최신순 출력
4. 최소 2개 클래스, 3개 모듈, dataclass, 타입 힌트
5. 실제 적용된 데코레이터 1개 이상
6. 잘못된 날짜·금액·타입·카테고리·id 처리
7. 오류 시 스택트레이스 미노출과 0이 아닌 종료 코드
8. README의 실행법·저장 형식·명령 예시·CSV 스키마
9. Python 3.10 이상과 표준 라이브러리만 사용
10. 수정·삭제 시 파일 데이터 안전성

## 해결 전략

### 구조

```text
CLI → BudgetService → TransactionRepository / CategoryStore / BudgetStore
             ↓
        Transaction(dataclass), SearchCriteria
```

- `models.py`: 데이터 구조와 검증 책임
- `repositories.py`: 파일 생성·읽기·원자적 재작성 책임
- `services.py`: CRUD·검색·요약·CSV 업무 규칙
- `cli.py`: 명령 분석, 대화형 입력, 사용자 출력
- `decorators.py`: 명령 로그와 실행 시간 측정

### 데이터 안전성과 스트리밍

거래 파일은 날짜 오름차순으로 유지한다. 목록·검색에서는 파일 끝부터 일정
크기의 바이트 블록을 읽고 한 줄씩 `yield`하므로 전체 거래 파일을 메모리에
적재하지 않으면서 최신순 결과를 만든다. 수정·삭제·정렬이 필요한 저장 작업은
같은 폴더의 임시 파일에 먼저 기록하고 `fsync()` 후 `os.replace()`로
교체하여 중간 실패로 원본이 반쯤 쓰이는 위험을 줄였다.

### 모호한 부분과 판단 근거

1. PDF 3쪽은 옵션을 `--`로 통일하라고 하고 모든 예시도 `--limit`,
   `--from` 등을 사용한다. 8쪽 추출 텍스트의 “`-`로 통일”은 HTML/PDF 변환
   과정에서 하이픈 하나가 유실된 것으로 판단하여 **긴 옵션은 `--`**로 구현했다.
2. `update`는 옵션 방식과 대화형 방식 중 하나를 택할 수 있으므로 자동화와
   일관성이 좋은 **옵션 방식**으로 고정했다.
3. 카테고리 초기 상태는 안 A/안 B 중 선택 가능하므로 즉시 사용할 수 있는
   **기본 카테고리 자동 생성(안 A)**을 선택했다.
4. “최신순”은 생성 시각 필드가 필수 모델에 없으므로 **거래 날짜 내림차순**으로
   정의했다. 같은 날짜에서는 id 내림차순으로 일관된 결과를 만든다.
5. 가져오기 오류 행의 세부 정책은 명시되지 않아 정상 행은 저장하고 잘못된
   행은 `skipped`로 집계하는 부분 성공 방식을 택했다. 필수 헤더나 파일 자체의
   문제는 작업 전체 오류로 처리한다.

## 구현 결과물

- 실행 애플리케이션: `budget_app/`
- 자동 테스트: `tests/test_budget_app.py`
- 실행·저장·명령 설명: `README.md`
- 가져오기 예제: `sample_import.csv`
- 제출 보고서: 현재 `REPORT.md`

필수 10개 기능, 3개 JSONL 저장 파일, 기본 카테고리 초기화, 제너레이터,
실행 로그 데코레이터, 타입 힌트, 원자적 수정·삭제를 모두 구현했다.

## 실행 방법

프로젝트 루트에서 다음 순서로 확인한다.

```bash
python -m budget_app --help
python -m budget_app category list
python -m budget_app import --from sample_import.csv
python -m budget_app budget set --month 2026-07 --amount 500000
python -m budget_app summary --month 2026-07 --top 3
python -m budget_app list --limit 5
python -m budget_app export --out july.csv --month 2026-07
python -m unittest discover -v
```

기존 데이터와 분리해 시연하려면 모든 명령에서 하위 명령 앞에
`--data-dir ./demo_data`를 넣는다.

## 검증 결과

자동 테스트가 확인하는 항목은 다음과 같다.

1. 저장 파일 3개 자동 생성
2. 거래 추가·최신순 목록·검색·수정·삭제
3. 월별 합계, 예산 사용률, 초과 경고 판단
4. 사용 중 카테고리 삭제 차단
5. 잘못된 날짜와 금액 거부
6. CSV 정상/오류 행 가져오기와 내보내기
7. JSONL 날짜 정렬
8. 없는 id 오류의 비정상 종료 코드와 스택트레이스 미출력

최종 검증 결과:

```text
Ran 8 tests in 0.644s
OK
```

추가로 실제 CLI에서 예제 CSV 3건 가져오기, 50,000원 예산 설정, 월 요약,
최신순 목록, CSV 3건 내보내기를 실행했다. 월 지출 67,000원, 사용률 134.0%,
예산 초과 경고가 예상대로 출력되었고 모든 정상 명령은 종료 코드 0이었다. 없는
거래 id 삭제는 오류·힌트와 종료 코드 2를 반환했으며 스택트레이스는 표시하지
않았다.

## 추가 개선 아이디어

1. 대규모 CSV 가져오기는 현재 행마다 안전하게 재작성하므로 느릴 수 있다.
   검증 완료 행을 배치로 원자적 저장하면 성능을 높일 수 있다.
2. 거래에 `created_at`을 추가하면 같은 날짜 거래도 실제 생성 시각 기준으로
   정렬할 수 있다.
3. 백업·복구 명령과 반복 거래 기능을 추가하면 PDF의 보너스 요구까지
   충족할 수 있다.
4. 파일 잠금을 추가하면 여러 프로세스가 동시에 실행될 때의 쓰기 충돌을
   방지할 수 있다.
